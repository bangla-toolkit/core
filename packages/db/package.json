{
  "name": "@bntk/db",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "main": "index.ts",
  "prisma": {
    "schema": "./schemas",
    "seed": "bun seed.ts"
  },
  "scripts": {
    "reset": "prisma migrate reset --force",
    "deploy": "prisma migrate deploy",
    "migration": "prisma migrate dev --skip-generate",
    "studio": "prisma studio",
    "seed": "prisma db seed",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "prisma": "^6.4.1"
  }
}

# removeSuffix()

```ts
function removeSuffix(word): string;
```

Defined in: [stemming.ts:46](https://github.com/nurulhudaapon/bntk/blob/314d9b2ac599759411d348b48ba4fdc0d09954c0/packages/core/stemming/src/stemming.ts#L46)

Removes matching suffix from the end of a word

## Parameters

| Parameter | Type     | Description                    |
| --------- | -------- | ------------------------------ |
| `word`    | `string` | The word to remove suffix from |

## Returns

`string`

The word with suffix removed if matched, otherwise original word
